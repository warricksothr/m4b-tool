# Paths tracked on `main` that must NOT appear on `release`.
# Stripped from the index and worktree after every
# `git merge --squash main` (and on the initial orphan cut).
#
# See AGENTS.md (on `main`) and the `release-cut` skill for the
# mechanism. .gitignore-style patterns; trailing `/` denotes a directory.

# Agent orientation — only relevant during development on `main`.
CLAUDE.md
AGENTS.md
.claude/

# Develop-side README — `release` carries its own consumer-focused README.
README.md

# Forgejo-side CI workflows. Tests/lint run only on the development
# host, not on the public mirror — see AGENTS.md "CI/CD layout".
.forgejo/

# Renovate config. Renovate runs on the development host (Forgejo),
# never against the public mirror, so the config has no business
# being on `release`.
renovate.json
